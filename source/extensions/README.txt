Put here your Sphinx extensions. You will then need to import the path to this directory
from conf.py, for example:

    import os
    import sys
    sys.path.insert(0, os.path.abspath('extensions'))
